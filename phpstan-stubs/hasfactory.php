<?php

namespace Illuminate\Database\Eloquent\Factories;

/**
 * Lightweight stub to teach PHPStan that HasFactory accepts a factory generic.
/**
 * Lightweight stub for HasFactory used by PHPStan.
 *
 * Note: kept non-generic here to avoid requiring all models to declare the
 * factory generic in their docblocks. If you prefer strict typing for
 * HasFactory<TFactory> reintroduce the @template TFactory and add
 * `@phpstan-use` annotations to each model.
 */
trait HasFactory {}
